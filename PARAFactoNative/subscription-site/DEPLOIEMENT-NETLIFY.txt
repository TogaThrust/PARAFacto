================================================================================
  PARAFacto — Dossier à connecter à Netlify (racine = ce dossier subscription-site)
================================================================================

CONTENU
-------
  netlify.toml          Paramètres build / functions
  package.json          Dépendance stripe (npm install au build)
  netlify/functions/    API license-check + session-customer
  public/               Site statique (index, success, installateur)

AVANT LE PREMIER DÉPLOIEMENT
----------------------------
  1. Dans public/index.html, remplacez l’URL du bouton « Souscrire » :
       https://buy.stripe.com/REMPLACEZ_VOTRE_LIEN
     par votre vrai Payment Link ou lien Checkout Stripe.

  2. Dans Stripe, URL de succès du paiement :
       https://VOTRE-SITE.netlify.app/success.html?session_id={CHECKOUT_SESSION_ID}

MÉTHODE RECOMMANDÉE (Git + Netlify)
-----------------------------------
  - Poussez le dépôt sur GitHub/GitLab.
  - Netlify : Add new site > Import from Git.
  - Base directory : subscription-site
    (dépôt github.com/TogaThrust/PARAFacto : la racine du repo contient subscription-site)
  - Build command : npm install
  - Publish directory : public
  - Les functions sont prises depuis netlify/functions (voir netlify.toml).

  - Site settings > Environment variables :
      STRIPE_SECRET_KEY = sk_live_... (ou sk_test_... en test)

  - Webhook checkout (fonction stripe-webhook.js) — au moins UN des filtres suivants doit matcher :
      A) PARAFACTO_PAYMENT_LINK_IDS = id(s) du Payment Link Stripe, forme plink_xxxxxxxx
         (Stripe > Liens de paiement > ouvrir le lien > copier l’ID, ex. dans l’URL du tableau de bord).
         Si vous utilisez buy.stripe.com/..., c’est en général SUFFISANT : pas besoin de PARAFACTO_PRICE_IDS.
      B) PARAFACTO_PRICE_IDS = price_xxx,price_yyy (liste des price autorisés sur la session Checkout).
      C) Métadonnées sur la session / le lien : product_line=parafacto_native OU app=parafacto OU brand=parafacto
      Exemple prix (si vous ajoutez B) : promo 19,99 EUR price_1TNE3n1Uq3SAnIi7m8Y965Z6 + standard 29,99 EUR
        price_1TNE4U1Uq3SAnIi7edfHWutA ; retirer l’ancien price_1TJfyS1Uq3SAnIi7lS49bvvo.

IMPORTANT — GLISSER-DÉPOSER SEUL
---------------------------------
  Un déploiement manuel en ne déposant QUE le dossier « public » ne publie PAS
  les fonctions serverless (license-check, session-customer). L’app Windows ne
  pourra pas vérifier l’abonnement. Utilisez Git ou la CLI Netlify (voir ci-dessous).

INSTALLATEUR .EXE (> 100 Mo) — NE PAS METTRE DANS LE DÉPÔT GIT
--------------------------------------------------------------
  GitHub refuse les fichiers > 100 Mo dans le code. Le .exe est ignoré par Git
  (voir .gitignore : public/files/*.exe).

  Solution simple — GitHub Releases (fichier joint, hors arborescence du repo) :
    1. Sur github.com/TogaThrust/PARAFacto → Releases → Create a new release.
    2. Tag ex. v1.0.0, joindre PARAFactoNative_Installer.exe en « binaire ».
    3. Après publication, copiez l’URL directe du fichier (bouton droit sur le
       nom du .exe dans la release → copier l’adresse du lien).
    4. Dans public/index.html, remplacez le href du bouton « Télécharger » par
       cette URL (au lieu de ./files/...).

  Variante : lien OneDrive / Google Drive / autre hébergeur de fichiers.

INSTALLATEUR .EXE (RECOMMANDÉ : GITHUB RELEASES)
------------------------------------------------
  Le bouton "Télécharger" pointe vers :
    https://github.com/TogaThrust/PARAFacto/releases/latest/download/PARAFactoNative_Installer.exe

  Workflow conseillé à chaque version :
    1) Exécuter .\CreerInstalleur.ps1
    2) Récupérer : PARAFactoNative\installer_output\PARAFactoNative_Installer.exe
    3) Créer une GitHub Release (tag vX.Y.Z)
    4) Joindre ce fichier comme asset avec le NOM EXACT :
         PARAFactoNative_Installer.exe

CLI NETLIFY (alternative)
-------------------------
  Depuis ce dossier (subscription-site), après npm install :
    npx netlify login
    npx netlify init
    npx netlify deploy --build --prod

URL À METTRE DANS subscription_config.json (application Windows)
-----------------------------------------------------------------
  licenseCheckApiUrl :
    https://VOTRE-SITE.netlify.app/.netlify/functions/license-check

  paymentPageUrl :
    URL du Payment Link Stripe actif (Produits > Liens de paiement > copier),
    du type https://buy.stripe.com/...
    Elle doit pointer vers l’offre avec les price_ ci-dessus (promo puis standard).

================================================================================
