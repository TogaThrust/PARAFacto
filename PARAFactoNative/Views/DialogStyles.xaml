<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Charte commune des petites fenêtres modales (factures, agenda, choix) -->
    <Style x:Key="DialogMessageTextBlock" TargetType="TextBlock">
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="#334155"/>
        <Setter Property="LineHeight" Value="22"/>
    </Style>
    <Style x:Key="DialogCaptionTextBlock" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="#0F172A"/>
    </Style>
    <Style x:Key="DialogFieldLabelTextBlock" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="#475569"/>
    </Style>
    <Style x:Key="DialogReadOnlyValueTextBlock" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="#0F172A"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style x:Key="DialogSectionHeaderTextBlock" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="#0F172A"/>
    </Style>
    <Style x:Key="DialogMutedLabelTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="#64748B"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <!-- Formulaire dans une fenêtre à taille fixe : le contenu défile sans sortir du cadre -->
    <Style x:Key="DialogFormScrollViewer" TargetType="ScrollViewer">
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="CanContentScroll" Value="False"/>
    </Style>
    <Style x:Key="DialogChoiceButton" TargetType="Button">
        <Setter Property="Margin" Value="0"/>
        <Setter Property="MinWidth" Value="112"/>
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Padding" Value="14,8"/>
    </Style>
    <Style x:Key="DialogPrimaryChoiceButton" TargetType="Button" BasedOn="{StaticResource DialogChoiceButton}">
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style x:Key="DialogStackActionButton" TargetType="Button">
        <Setter Property="Margin" Value="0,0,0,8"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
    </Style>
</ResourceDictionary>
