<Window x:Class="PARAFactoNative.Views.UnavailabilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Indisponibilité"
        Height="540" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Style="{StaticResource DialogMessageTextBlock}"
                   Text="Définissez une plage horaire pendant laquelle vous ne pouvez pas recevoir de rendez-vous. Elle apparaît en rouge dans le calendrier."/>
        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Style="{StaticResource DialogSectionHeaderTextBlock}" Margin="0,0,0,4" Text="Date"/>
            <DatePicker x:Name="DatePick" SelectedDateChanged="DatePick_OnSelectedDateChanged"/>
        </StackPanel>
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Style="{StaticResource DialogSectionHeaderTextBlock}" Margin="0,0,0,4" Text="Heure de début"/>
                <ComboBox x:Name="StartCombo" IsEditable="False" SelectionChanged="StartCombo_OnSelectionChanged"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock Style="{StaticResource DialogSectionHeaderTextBlock}" Margin="0,0,0,4" Text="Heure de fin"/>
                <ComboBox x:Name="EndCombo" IsEditable="False"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Style="{StaticResource DialogSectionHeaderTextBlock}" Margin="0,0,0,4" Text="Raison (optionnel)"/>
            <TextBox x:Name="ReasonBox" AcceptsReturn="False" MaxLength="200"/>
        </StackPanel>
        <GroupBox Grid.Row="4" Header="Indisponibilités ce jour" Margin="0,14,0,8" Padding="8,8,8,8">
            <ListBox x:Name="UnavailList" Margin="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Display}" TextWrapping="Wrap" Style="{StaticResource DialogMessageTextBlock}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Supprimer la sélection" Style="{StaticResource DialogChoiceButton}" Margin="0,0,8,0" Click="Delete_Click"/>
            <Button Content="Ajouter" Style="{StaticResource DialogPrimaryChoiceButton}" Margin="0,0,8,0" Click="Add_Click"/>
            <Button Content="Fermer" Style="{StaticResource DialogChoiceButton}" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
